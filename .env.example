# Phase 0 does not require environment variables.
#
# Supabase and application settings will be introduced in Phase 1. Keep real
# values in .env.local and in Vercel project settings; never commit secrets.
